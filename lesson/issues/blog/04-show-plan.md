# B4 — Кнопка «Весь план»

## Что происходит сейчас
Виден только текущий шаг, но нельзя посмотреть весь маршрут.

## Как должно работать
Кнопка показывает задачи по порядку с отметками `✅`, `👉` и `⏳`.

## Как воспроизвести
Создай Скрибу, получи план и выполни первый шаг.

## С чего начать
Открой `BlogLesson.java`, блок `ISSUE B4`.

## Что сделать
Добавь кнопку `action:show_plan` и вызови готовый метод `showPlan`.

## Подсказки
1. Повтори форму информационной кнопки другого пути. 2. Обработчик возвращает `true`. 3. Данные менять не нужно.

## Готово, если
Порядок и отметки верны, XP прежний и `./gradlew test --tests '*LessonRouteContractTest.blogShowsFullPlan'` проходит.
