# Magic Pet

Magic Pet — Telegram-бот с искусственным интеллектом и тремя магическими питомцами:

- **Учёба** — совёнок Руни;
- **Спорт** — дракончик Игнис;
- **Личный блог** — лисёнок Скриба.

Пользователь выбирает тему, даёт питомцу имя и пишет свою цель. Бот составляет план, выдаёт задания и начисляет опыт. Питомец повышает уровень и меняется вместе с пользователем.

На пробном уроке студент не создаёт программу с пустого экрана. Он работает как разработчик в готовом продукте: исследует бот, берёт понятную задачу из GitHub, меняет несколько строк и сразу видит результат в Telegram.

## Студенту и менеджеру

Основная пошаговая инструкция находится в [методичке пробного урока](https://www.notion.so/3e16ba2ce4e8801a9c67c1830814439b). Идите по ней сверху вниз.

Актуальные задания находятся в [GitHub Issues](https://github.com/Pixel-Point-Studio/trial-lesson-magic-pet/issues). В каждой теме есть две ошибки и две идеи для улучшения. Все задачи можно выполнять независимо друг от друга.

### Подготовить отдельную папку занятия

Одинаково на macOS, Windows и Linux:

1. Откройте исходную папку `trial-lesson-magic-pet` в Visual Studio Code.
2. Откройте верхнее меню **Terminal → Run Task… / Терминал → Выполнить задачу…**.
3. Выберите **Magic Pet: подготовить пробный урок**.
4. Дождитесь, пока Visual Studio Code откроет новое окно с готовой папкой и файлом `README.md`.

Вводить имя студента и заранее выбирать тему не нужно. В новой папке одновременно доступны все три темы и все 12 независимых задач. Студент может взять задачи из разных тем.

Технический запасной способ для macOS и Linux:

```bash
./gradlew prepareLesson
```

Для Windows: `gradlew.bat prepareLesson`.

### Подключить Telegram и запустить бот

1. Откройте файл `.env`.
2. Вставьте Telegram-токен после `TELEGRAM_BOT_TOKEN=` и сохраните файл.
3. Откройте в Visual Studio Code раздел **Запуск и отладка**.
4. Выберите **Magic Pet: запустить и отлаживать**.
5. Отправьте своему боту `/start`.

Настройки школьного AI обычно уже перенесены автоматически. Если AI временно недоступен, бот сам использует готовый резервный план.

Чтобы начать путь пользователя заново, отправьте боту `/reset`, затем `/start`.

### Проверить одну задачу

В карточке GitHub указан её код: `S1`–`S4`, `I1`–`I4` или `B1`–`B4`. Выполните, например:

```bash
./scripts/check-task.sh S1
```

Успешная проверка заканчивается понятным сообщением на русском. Чтобы увидеть собственные изменения, откройте раздел **Source Control / Контроль версий** в Visual Studio Code: добавленные строки будут зелёными, удалённые — красными.

## Владельцу продукта

Техническая архитектура, первоначальная настройка компьютеров, школьный AI, секреты, восстановление и полная приёмка вынесены в отдельную [техническую инструкцию владельца продукта](https://www.notion.so/3e26ba2ce4e8813b9fedce0a5342f657). Эти детали не нужно объяснять менеджеру или студенту.

Основные служебные проверки владельца:

```bash
./scripts/check-computer.sh
./scripts/test-prepare-lesson.sh
./scripts/final-acceptance.sh
```

`final-acceptance.sh` — проверка новой версии продукта перед выпуском. Менеджер не запускает её перед занятием.

Для автоматического переноса школьных настроек AI создайте локальный `.env.lesson-secrets` по примеру `.env.lesson-secrets.example`. Этот файл и настоящие ключи не попадают в Git. Telegram-токен намеренно добавляется отдельно для каждого урока.

## Структура документации

- [product-spec.md](product-spec.md) — продуктовая спецификация;
- [technical-spec.md](technical-spec.md) — техническая спецификация;
- [implementation-plan.md](implementation-plan.md) — история и план реализации;
- [acceptance](acceptance) — материалы внутренней приёмки;
- [scripts](scripts) — внутренние автоматические проверки владельца продукта.
